#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcmp - compares two strings
 *@s1: parameter of type character
 *@s2: parameter of type parameter
 * Return: Always 0
 */
int _strcmp(char *s1, char *s2)
{
	int z = strcmp(s1, s2);

	return (z);
}
