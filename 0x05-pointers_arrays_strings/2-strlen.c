#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *int_strlen - returns the length of a string
 *
 * @s: parameter of type character
 * Return: Always 0
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}


